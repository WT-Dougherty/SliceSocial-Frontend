import {View, Text, ScrollView} from 'react-native';

function FeedScreen() {
    return (
        <ScrollView>
            <Text>
                Feed
            </Text>
        </ScrollView>
    )
}

export default FeedScreen;